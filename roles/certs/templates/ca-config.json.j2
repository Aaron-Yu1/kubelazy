{
    "signing": {
        "default": {
            "expiry": "{{ CERT_EXPIRY }}"
        },
        "profiles": {
            "kubernetes": {
                "expiry": "{{ CERT_EXPIRY }}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "server auth",
		    "client auth"
                ]
            },
            "client": {
                "expiry": "{{ CUSTOM_EXPIRY }}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "client auth"
                ]
            }
        }
    }
}
